import './globals.css';

export const metadata = {
  title: 'VibeLeak AI | Relationship, Personality & Future Analysis',
  description: 'Personalized entertainment tests with separate questionnaires for Ex Comeback, Crush Compatibility, Toxicity, Rarity and Future Rich/Broke.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
